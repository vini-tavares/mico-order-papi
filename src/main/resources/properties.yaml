# Contains Properties Independent from Environment

# Mico Order PAPI
# resource::cd8b296d-8848-4a91-98ed-4a47f73703bd:mico-order-papi:1.0.5:raml:zip:mico-order-papi.raml

api:
  groupId: "${api.groupId}"
  artifactId: "${api.artifactId}"
  version: "${api.version}"
  spec: "resource::${api.groupId}:${api.artifactId}:${api.version}:raml:zip:${api.artifactId}.raml"
  majorVersion: "v1"